<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.zzp.albumimageselect">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application android:name="qrcode.ecar.com.albumlib.AppContext"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <activity android:name="qrcode.ecar.com.albumlib.PickBigImagesActivity"/>
        <activity android:name="qrcode.ecar.com.albumlib.PickOrTakeImageActivity">
        </activity>

    </application>

</manifest>
